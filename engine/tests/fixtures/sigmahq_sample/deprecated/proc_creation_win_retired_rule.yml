# SigmaHQ keeps retired rules under deprecated/. The importer prunes that
# directory, so this file must never appear in an import result.
title: Retired Rule That Must Not Be Imported
id: 0a0a0a0a-0000-4000-8000-000000000004
status: deprecated
description: Superseded detection kept only for history.
logsource:
  category: process_creation
  product: windows
detection:
  selection:
    Image|endswith: '\retired.exe'
  condition: selection
level: informational
